defmodule SurfBoard.Browser.Lightpanda do
  @moduledoc false

  # One of the dimension modules on a driver Spec (`spec.browser`) —
  # currently just a marker distinguishing Lightpanda from real Chrome
  # for anything that needs to branch on vendor rather than protocol
  # (CDP vs BiDi) later.
end
